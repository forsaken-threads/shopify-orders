NICKNAME=shopify-orders
SOURCE_UID=$(id -u)
SOURCE_GID=$(id -g)
SERVE_PORT_1=7801

# These show the "hidden" local development defaults, but they can be overridden for other deployment types.
# BUILD_TARGET=base
# DO_INTERACTIVE='-it'
# VOLUMES_ALIAS=local-development-volumes
